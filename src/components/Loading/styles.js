import styled from 'styled-components';

const LoadingIndicator = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate3d(-50%, -50%, 0);
  font-size: 24px;
  font-weight: bold;
`;

export {
  LoadingIndicator  
};
